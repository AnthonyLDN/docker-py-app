pipeline {
    agent any
    def xxx="${env.JENKINS_HOME}"
    stages {
        stage('Example') {
            steps {
                echo "Running ${env.BUILD_ID} on ${xxx}"
            }
        }
    }
}